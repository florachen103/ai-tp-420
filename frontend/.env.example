# 方式 A（推荐 Vercel）：仅后端根 URL，无 /api/v1；与 src/middleware.ts 同源转发配合
# BACKEND_ORIGIN=https://你的服务.onrender.com

# 方式 B：浏览器直连后端（须后端 CORS）；必须以 /api/v1 结尾
# NEXT_PUBLIC_API_BASE_URL=https://你的服务.onrender.com/api/v1

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
